class Transaction {
  Transaction(this.amount, this.remark, this.category, this.balance,
      this.transactionDate);
  String remark;
  String category;
  double balance;
  DateTime transactionDate;
  double amount;
}
